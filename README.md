![example workflow](https://github.com/MatthewSerre/ruby_scryfall/actions/workflows/ruby.yml/badge.svg)

# ruby_scryfall

`ruby_scryfall` is a gem for consuming the Scryfall REST-like API. This gem is a community project aimed at providing a tool for Ruby developers to interact with the Scryfall API and is neither endorsed nor sponsored by Scryfall. Please consult [the Scryfall API documentation](https://scryfall.com/docs/api) for terms of use.

## Installation

Add `ruby_scryfall` to your `Gemfile` and run `bundle` or run `gem install ruby_scryfall` in your terminal.

## Usage

TODO

## Development

After checking out the repo, run `bin/setup` to install dependencies. Then, run `rake spec` to run the tests. You can also run `bin/console` for an interactive prompt that will allow you to experiment.

To install this gem onto your local machine, run `bundle exec rake install`. To release a new version, update the version number in `version.rb`, and then run `bundle exec rake release`, which will create a git tag for the version, push git commits and the created tag, and push the `.gem` file to [rubygems.org](https://rubygems.org).

## Contributing

Bug reports and pull requests are welcome on GitHub at https://github.com/MatthewSerre/ruby_scryfall.

## License

The gem is available as open source under the terms of the [MIT License](https://opensource.org/licenses/MIT).

